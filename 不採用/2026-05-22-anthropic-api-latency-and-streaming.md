---
title: "Anthropic API のレスポンス時間の目安とストリーミングの効果"
date: 2026-05-22
tags: [anthropic, claude-api, latency, streaming]
format: blog-post
---

## はじめに

Claude を組み込んだアプリケーションを設計するとき、最初に気になるのは「実際どのくらいの速さで返ってくるのか」という点である。本記事では 2026 年 5 月時点で公開されているベンチマークをもとに、Anthropic API のレスポンス時間の目安をモデル別に整理し、あわせて体感速度を大きく左右する「ストリーミング」の仕組みと効果についてまとめる。

## レスポンス時間に関する主要指標

API レイテンシを語るときに頻出する 3 つの指標を先に整理しておく。

- **TTFT (Time To First Token)**: リクエスト送信から最初のトークンが返るまでの時間
- **TPS (Tokens Per Second)**: 1 秒あたりに生成されるトークン数
- **総応答時間**: 完了するまでの所要時間（≒ TTFT + 出力トークン数 / TPS）

体感速度を決めるのは主に TTFT、生成全体にかかる時間を決めるのは TPS、という分担になる。

## モデル別レスポンス時間（2026 年時点）

| モデル | TTFT 中央値 | 生成速度 (tok/s) | 500 語応答の目安 |
|---|---|---|---|
| Haiku 4.5 | 約 600ms 以下 | 80〜120 | 2〜3 秒 |
| Sonnet 4.6 | 500〜800ms（P95: 約 900ms） | 40〜60 | 4〜5 秒 |
| Opus 4.6 | 1〜2 秒 | 20〜30 | 8〜10 秒 |

特筆すべきは Anthropic の**レイテンシの一貫性**で、P95/P50 比が約 1.8x とフロンティアモデル提供各社の中でもっとも安定している。つまり「ときどき極端に遅い」現象が少ない。

用途別の指針はおおむね次のようになる。

- **速度最優先**: Haiku（チャット応答、検索補助、軽量分類）
- **品質と速度のバランス**: Sonnet（汎用アシスタント、コード生成）
- **推論品質最優先**: Opus（複雑な計画、難解な推論、エージェント）

ここまでで分かったこと: モデル選定は「ベンチマーク数値の最強」ではなく、TTFT と TPS のトレードオフを用途に合わせて選ぶ作業である。

## ストリーミングとは何か

ストリーミングとは、API のレスポンスを **一括ではなくトークンごとに少しずつ受け取る** 方式である。Anthropic API では `stream=true` を指定する（または SDK の `client.messages.stream(...)` を使う）と、Server-Sent Events (SSE) でトークンが逐次配信される。

```text
通常（非ストリーミング）:
  リクエスト送信 → [全部生成し終わるまで待機] → 完成した文章が一気に返る

ストリーミング:
  リクエスト送信 → 最初のトークンが届く → 続けて少しずつ届く → 完了
```

Python SDK のシンプルな例:

```python
import anthropic

client = anthropic.Anthropic()

with client.messages.stream(
    model="claude-sonnet-4-6",
    max_tokens=1024,
    messages=[{"role": "user", "content": "Hello"}],
) as stream:
    for text in stream.text_stream:
        print(text, end="", flush=True)
```

## ストリーミングの効果

### 1. 体感速度が劇的に向上する

たとえば Opus 4.6 で 500 語生成するケースでは、完成までは 8〜10 秒かかるが、最初の文字は 1〜2 秒で表示される。Claude.ai や ChatGPT で文字がタイプライターのように出てくる UX はこの仕組みによるもの。

### 2. 総生成時間そのものは変わらない

生成にかかる秒数自体は変わらない。あくまで「届き方」を変えてユーザーの待たされ感を消す手段である。料金（トークン課金）にも影響しない。

### 3. 長文生成の実用性が上がる

- HTTP 接続が長時間アイドルにならないためタイムアウトを回避できる
- 出力途中で内容が不適切と分かれば早期キャンセルできる
- 部分パースが可能（コード生成中にシンタックスハイライト、Markdown のプログレッシブレンダリングなど）

### 4. デメリットと注意点

- **JSON など構造化出力では恩恵が小さい**: 完成までパースできないため、ストリームの途中で使えない
- **エラーハンドリングが複雑化**: 途中で接続が切れたケースのリトライ・部分結果の扱いを設計する必要がある
- **クライアント側の実装が増える**: イベントごとの差分適用ロジックが必要

### 使い分けの目安

| ケース | ストリーミング |
|---|---|
| チャット UI・対話アプリ | 必須レベルで有効 |
| 長文記事・コード生成 | 有効 |
| 短い分類タスク（数十トークン） | 効果薄 |
| JSON / 構造化出力をそのまま使う | 不要なことが多い |

## まとめ

- Claude API のレスポンス時間は Haiku < Sonnet < Opus の順で、TTFT・TPS の両方で大きな差がある
- Anthropic はレイテンシの分散が小さく、本番運用での予測可能性が高い
- ストリーミングは総時間を縮めるわけではないが、TTFT が支配的な体感速度を大幅に改善する
- ユーザーが画面の前で待つ UI では、ストリーミングを入れない理由がほぼない
- 一方で構造化出力をパースして使う用途では無理に有効化する必要はない

リアルタイムの計測値を確認したいときは [Artificial Analysis](https://artificialanalysis.ai/) や [AILatency](https://www.ailatency.com/) のダッシュボードが便利。公式の遅延削減ガイドは [Reducing latency - Claude API Docs](https://docs.anthropic.com/en/docs/test-and-evaluate/strengthen-guardrails/reduce-latency) にまとまっている。
